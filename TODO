* Helpers API
  - Something to add both config_validator and on_terminate and tokio extension for that
* Run on spirit itself too
* Example inside readme, better explanation
* Fill in TODOs in docs
* Contexts for errors
* Ability to manipulate callbacks at runtime
* More logging configuration and targets
* Network logging reconnects
* Metrics
* support for capabilities
* Features
* Helpers with set-like configuration, like sockets
* Ose daemonize from crate once available (or integrate)
